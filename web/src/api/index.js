import domain from './domain'
import suffix from './suffix'
import whois from './whois'
import system from './system'

const api = {
  domain,
  suffix,
  whois,
  system
}

export default api