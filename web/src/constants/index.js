import errorMessage from './errorMessage'
import regexp from './regexp'
import status from './status'
import whoisStatus from './whoisStatus'

const constants = {
  errorMessage,
  regexp,
  status,
  whoisStatus,
  invalidTime: '0001-01-01T00:00:00Z'
}

export default constants